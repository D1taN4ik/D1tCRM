services:
  db:
    image: postgres:16
    container_name: d1tcrm-db
    restart: always
    environment:
      POSTGRES_DB: d1tcrm
      POSTGRES_USER: d1tcrm_user
      POSTGRES_PASSWORD: d1tcrm_password
    ports:
      - "5432:5432"
    volumes:
      - d1tcrm_pgdata:/var/lib/postgresql/data

volumes:
  d1tcrm_pgdata: